import { Config } from 'tailwindcss';

const config: Partial<Config> = {
    purge: [],
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}

export default config;
